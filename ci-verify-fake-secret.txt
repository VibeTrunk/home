# Throwaway file to verify gitleaks CI actually catches a secret.
# This is AWS's own official example access key ID from their docs — not a
# real credential. This file and its containing commit will be reverted.
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
